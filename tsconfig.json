{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "skipLibCheck": true,
    "types": [
      "node"
    ]
  },
  "include": [
    "src",
    "scripts"
  ],
  "//": "These reach into app, and app spells its imports with a .ts extension under bundler resolution, which NodeNext will not read. tsconfig.scripts.json checks them instead.",
  "exclude": [
    "scripts/buildSimTables.ts",
    "scripts/partialGameProbe.ts",
    "scripts/simAgreement.ts"
  ]
}
